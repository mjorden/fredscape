% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/market.R
\name{demand}
\alias{demand}
\alias{linear_demand}
\alias{demand_fn}
\title{Market demand}
\usage{
linear_demand(intercept, slope)

demand_fn(p_of_q, q_max)
}
\arguments{
\item{intercept}{Choke price \eqn{a}: the price at which demand is zero.}

\item{slope}{\eqn{b}: how fast price falls per unit of quantity. Positive.}

\item{p_of_q}{A function of one argument returning the price at that
quantity. Must be decreasing.}

\item{q_max}{The largest quantity worth considering (for example, where
price reaches zero). Numerical searches are confined to \verb{[0, q_max]}.}
}
\value{
An object of class \code{demand} (and \code{linear_demand} or
\code{general_demand}).
}
\description{
The demand side of a market, written as inverse demand \eqn{P(Q)}: the
price at which buyers take quantity \eqn{Q}. \code{linear_demand()} is the
textbook \eqn{P = a - bQ} and has closed forms for everything below;
\code{demand_fn()} accepts any decreasing function of \code{q} and falls back to
numerical methods.
}
\examples{
d <- linear_demand(intercept = 100, slope = 1)
d
price_at(d, 40)
marginal_revenue(d, 40)
elasticity(d, 40)

# Constant-elasticity demand, handled numerically
ce <- demand_fn(function(q) 400 / sqrt(q + 1), q_max = 1000)
marginal_revenue(ce, 15)
}
\seealso{
\code{\link[=price_at]{price_at()}}, \code{\link[=marginal_revenue]{marginal_revenue()}}, \code{\link[=consumer_surplus]{consumer_surplus()}}, and the
market structures \code{\link[=monopoly]{monopoly()}}, \code{\link[=cournot]{cournot()}}, \code{\link[=perfect_competition]{perfect_competition()}}.
}
