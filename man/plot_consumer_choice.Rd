% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-micro.R
\name{plot_consumer_choice}
\alias{plot_consumer_choice}
\title{Draw the consumer's choice in one call}
\usage{
plot_consumer_choice(
  u,
  b,
  levels = NULL,
  xlim = NULL,
  ylim = NULL,
  goods = c("Good x", "Good y"),
  title = NULL,
  subtitle = NULL,
  source = NULL,
  label_levels = TRUE,
  panel = "blue"
)
}
\arguments{
\item{u}{A function of \code{x} and \code{y}, typically from \code{\link[=cobb_douglas]{cobb_douglas()}}.}

\item{b}{A \code{\link[=budget]{budget()}}.}

\item{levels}{Curve levels. Defaults to the optimum's utility and two
curves either side of it.}

\item{xlim, ylim}{Panel limits. Default to a little beyond each intercept.}

\item{goods}{Axis labels for \code{x} and \code{y}.}

\item{title, subtitle, source}{Passed to \code{\link[=labs_econ]{labs_econ()}}. Sensible defaults
are filled in from \code{u} and \code{b}.}

\item{label_levels}{Print each curve's level at its right-hand end?}

\item{panel}{Passed to \code{\link[=theme_econ]{theme_econ()}}.}
}
\value{
A ggplot object. Add \code{\link[=econ_masthead]{econ_masthead()}} last if you want the block.
}
\description{
Composes indifference curves, the budget line and the optimal bundle into
a finished chart in the house style, with each curve labelled by its level
at the right-hand edge of the panel. For a \code{kind = "production"}
Cobb-Douglas the labels switch to isoquant and isocost vocabulary.
}
\examples{
u <- cobb_douglas(alpha = 0.3)
b <- budget(income = 100, px = 2, py = 5)
plot_consumer_choice(u, b)

f <- cobb_douglas(0.5, 0.5, A = 3, kind = "production")
plot_consumer_choice(f, budget(600, 20, 30), goods = c("Labour", "Capital"))
}
