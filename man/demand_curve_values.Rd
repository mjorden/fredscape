% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/market.R
\name{demand_curve_values}
\alias{demand_curve_values}
\alias{price_at}
\alias{quantity_at}
\alias{marginal_revenue}
\alias{elasticity}
\alias{consumer_surplus}
\title{Prices, quantities and revenue along a demand curve}
\usage{
price_at(d, q, ...)

quantity_at(d, p, ...)

marginal_revenue(d, q, ...)

elasticity(d, q, ...)

consumer_surplus(d, q, ...)
}
\arguments{
\item{d}{A demand object from \code{\link[=linear_demand]{linear_demand()}} or \code{\link[=demand_fn]{demand_fn()}}.}

\item{q}{Quantity. May be a vector.}

\item{...}{Passed on to methods.}

\item{p}{Price. May be a vector.}
}
\value{
A numeric vector.
\itemize{
\item \code{price_at()} -- the price at which \code{q} is demanded (inverse demand).
\item \code{quantity_at()} -- the quantity demanded at price \code{p}.
\item \code{marginal_revenue()} -- \eqn{P(q) + q P'(q)}, the extra revenue from
one more unit when the price must fall to sell it.
\item \code{elasticity()} -- the price elasticity of demand at \code{q},
\eqn{(dQ/dP)(P/Q)}; negative, and below \eqn{-1} where demand is
elastic.
\item \code{consumer_surplus()} -- the area under the demand curve and above the
price, up to \code{q}.
}
}
\description{
Prices, quantities and revenue along a demand curve
}
\examples{
d <- linear_demand(intercept = 100, slope = 1)
price_at(d, c(0, 40, 100))
quantity_at(d, 60)
marginal_revenue(d, 40)      # 100 - 2 * 40
elasticity(d, c(20, 50, 80)) # elastic above the midpoint
consumer_surplus(d, 40)
}
