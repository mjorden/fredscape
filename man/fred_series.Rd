% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/series.R
\name{fred_series}
\alias{fred_series}
\title{Download FRED series observations}
\usage{
fred_series(
  series_id,
  start = NULL,
  end = NULL,
  units = "lin",
  frequency = NULL,
  aggregation_method = "avg",
  key = fred_key()
)
}
\arguments{
\item{series_id}{Character vector of FRED series IDs, e.g. \code{"UNRATE"} or
\code{c("UNRATE", "GDPC1")}.}

\item{start, end}{Optional observation window. A \code{Date}, or anything
\code{\link[=as.Date]{as.Date()}} accepts. \code{NULL} means the full available history.}

\item{units}{Unit transformation applied by FRED before it returns the data:
\code{"lin"} (levels, the default), \code{"chg"} (change), \code{"ch1"} (change from a
year ago), \code{"pch"} (percent change), \code{"pc1"} (percent change from a year
ago), \code{"pca"} (compounded annual rate of change), \code{"cch"}, \code{"cca"} or
\code{"log"}.}

\item{frequency}{Optional lower frequency to aggregate to, e.g. \code{"m"}, \code{"q"}
or \code{"a"}. FRED can only aggregate downwards.}

\item{aggregation_method}{How to aggregate when \code{frequency} is set: \code{"avg"},
\code{"sum"} or \code{"eop"} (end of period).}

\item{key}{A FRED API key. Defaults to \code{\link[=fred_key]{fred_key()}}.}
}
\value{
A data frame with columns \code{series_id} (character), \code{date} (\code{Date})
and \code{value} (numeric), sorted by series then date. A series with no
observations in the requested window contributes no rows.
}
\description{
Fetches one or more series from the FRED \code{series/observations} endpoint and
returns them stacked in a single tidy data frame -- one row per series per
date -- which is the shape ggplot2 wants for a multi-series chart.
}
\details{
FRED encodes a missing observation as the string \code{"."}; those become \code{NA}
rather than a coercion warning.
}
\examples{
\dontshow{if (fredscape::fred_has_key()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
fred_series("UNRATE", start = "2000-01-01")

# Several series at once, as year-on-year percent change
fred_series(c("CPIAUCSL", "PCEPI"), start = "2015-01-01", units = "pc1")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=fred_series_info]{fred_series_info()}} for the metadata behind a series, and
\code{\link[=fred_search]{fred_search()}} to find an ID in the first place.
}
