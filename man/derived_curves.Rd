% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derived.R
\name{derived_curves}
\alias{derived_curves}
\alias{demand_curve}
\alias{engel_curve}
\alias{price_consumption_path}
\alias{income_consumption_path}
\title{Curves traced by the optimal bundle}
\usage{
demand_curve(u, b, prices, good = "x")

engel_curve(u, b, incomes, good = "x")

price_consumption_path(u, b, prices, good = "x")

income_consumption_path(u, b, incomes)
}
\arguments{
\item{u}{A function of \code{x} and \code{y}, typically from \code{\link[=cobb_douglas]{cobb_douglas()}} or one
of its siblings.}

\item{b}{A \code{\link[=budget]{budget()}} giving the baseline income and prices. The one being
varied is overridden.}

\item{prices}{Vector of prices for \code{good} to evaluate at.}

\item{good}{Which good's price to vary or quantity to report: \code{"x"} or
\code{"y"}.}

\item{incomes}{Vector of incomes to evaluate at.}
}
\value{
A data frame, one row per price or income:
\itemize{
\item \code{demand_curve()}: \code{price}, \code{quantity}, \code{good}.
\item \code{engel_curve()}: \code{income}, \code{quantity}, \code{good}.
\item \code{price_consumption_path()}: \code{price}, \code{x}, \code{y}, \code{good}.
\item \code{income_consumption_path()}: \code{income}, \code{x}, \code{y}.
}
}
\description{
\code{\link[=optimal_bundle]{optimal_bundle()}} answers one question: what does the consumer choose at
one set of prices and one income? These functions ask it repeatedly and
collect the answers.
}
\details{
\itemize{
\item \code{demand_curve()} varies the price of one good, holding the other price
and income fixed, and records the quantity of that good.
\item \code{engel_curve()} varies income, holding both prices fixed, and records the
quantity of one good.
\item \code{price_consumption_path()} and \code{income_consumption_path()} are the same
experiments, but keep the whole bundle \verb{(x, y)} so the locus can be drawn
on the indifference-curve diagram.
}

Every constructor in the package has a closed-form \code{\link[=optimal_bundle]{optimal_bundle()}}, so
these are fast; a plain \verb{function(x, y)} goes through the numeric line
search once per point.
}
\examples{
u <- cobb_douglas(alpha = 0.4)
b <- budget(income = 120, px = 3, py = 4)

# Cobb-Douglas demand is a rectangular hyperbola: x = 0.4 * 120 / px
demand_curve(u, b, prices = c(1, 2, 4, 8))

# ... and the Engel curve a ray through the origin
engel_curve(u, b, incomes = c(60, 120, 240))

# Quasi-linear: the demand for x ignores income entirely
q <- quasilinear(log, f_prime = function(x) 1 / x)
engel_curve(q, b, incomes = c(60, 120, 240))
}
