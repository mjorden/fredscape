% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/producer.R
\name{plot_producer_choice}
\alias{plot_producer_choice}
\title{Draw the producer's cost-minimising choice}
\usage{
plot_producer_choice(
  f,
  b,
  inputs = c("Labour", "Capital"),
  levels = NULL,
  xlim = NULL,
  ylim = NULL,
  title = NULL,
  subtitle = NULL,
  source = NULL,
  label_levels = TRUE,
  panel = "blue"
)
}
\arguments{
\item{f}{A production function of \code{x} and \code{y}.}

\item{b}{A \code{\link[=budget]{budget()}} read as an isocost line: \code{income} is the outlay and
\code{px}, \code{py} the input prices.}

\item{inputs}{Axis labels for the two inputs.}

\item{levels}{Curve levels. Defaults to the optimum's utility and two
curves either side of it.}

\item{xlim, ylim}{Panel limits. Default to a little beyond each intercept.}

\item{title, subtitle, source}{Passed to \code{\link[=labs_econ]{labs_econ()}}. Sensible defaults
are filled in from \code{u} and \code{b}.}

\item{label_levels}{Print each curve's level at its right-hand end?}

\item{panel}{Passed to \code{\link[=theme_econ]{theme_econ()}}.}
}
\value{
A ggplot object.
}
\description{
The same diagram as \code{\link[=plot_consumer_choice]{plot_consumer_choice()}} with the producer's
vocabulary: isoquants, an isocost line, and the input bundle that produces
the most for the outlay. Works for any production function, including a
plain \verb{function(x, y)}. The \code{kind} attribute is set to \code{"production"} on a
copy for the labels regardless of how the function was built -- so a
\code{cobb_douglas(0.5)} constructed with the default \code{kind = "utility"} draws
as a technology here without complaint. That is deliberate: the maths is
identical and the constructor default should not force a keystroke.
}
\examples{
f <- cobb_douglas(0.5, 0.5, A = 3, kind = "production")
plot_producer_choice(f, budget(600, 20, 30))
}
