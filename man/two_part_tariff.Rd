% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrimination.R
\name{two_part_tariff}
\alias{two_part_tariff}
\title{Two-part tariffs}
\usage{
two_part_tariff(demands, cost, n = 1)
}
\arguments{
\item{demands}{A demand object (one consumer type), or a list of them,
one per type, each describing a single consumer's demand.}

\item{cost}{A cost object.}

\item{n}{Number of consumers of each type. Recycled to the number of
types.}
}
\value{
An object of class \code{two_part_tariff}: a list with \code{price}, \code{fee},
\code{served} (logical, per type), \code{types} (a data frame: \code{type}, \code{n},
\code{served}, \code{quantity} per consumer, \code{surplus} per consumer after the
fee), \code{quantity} (total), \code{profit}, \code{marginal_cost}, and \code{candidates}
(the profit of each cut-off considered).
}
\description{
A seller who charges an entry fee plus a per-unit price. With identical
consumers the answer is Disneyland's: set the price at marginal cost and
the fee at the whole of each consumer's surplus, which replicates
\code{\link[=first_degree]{first_degree()}} profit with only two numbers.
}
\details{
With different types of consumer the seller faces a trade-off. Serving
everyone means pinning the fee to the \emph{lowest} type's surplus, which
pushes the optimal price above marginal cost so the higher types pay
more through the per-unit charge. Alternatively the seller can exclude
the low types and take the high types' full surplus. \code{two_part_tariff()}
evaluates every "serve this type and above" cut-off, optimises the price
for each, and returns the most profitable.
}
\examples{
d <- linear_demand(intercept = 100, slope = 1)
cst <- quadratic_cost(a = 20)

two_part_tariff(d, cst)                    # p = 20, fee = 3200

light <- linear_demand(intercept = 60, slope = 1)
two_part_tariff(list(light = light, heavy = d), cst, n = c(5, 1))  # serve both, p > MC
two_part_tariff(list(light = light, heavy = d), cst, n = c(1, 1))  # exclude light users
}
\seealso{
\code{\link[=plot_two_part_tariff]{plot_two_part_tariff()}}.
}
