% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{quasilinear}
\alias{quasilinear}
\title{Quasi-linear utility}
\usage{
quasilinear(f, f_prime = NULL)
}
\arguments{
\item{f}{A concave function of one argument.}

\item{f_prime}{Its derivative, or \code{NULL} to approximate numerically.}
}
\value{
A function of \code{x} and \code{y} of class \code{quasilinear}, with \code{f},
\code{f_prime} and \code{kind = "utility"} as attributes.
}
\description{
Builds \eqn{u(x, y) = f(x) + y} for a concave \code{f}: utility is linear in \code{y},
so \code{y} absorbs all income effects and the demand for \code{x} depends on prices
alone (as long as the consumer can afford the interior optimum). The
textbook case is \code{f = log}.
}
\details{
Demand solves \eqn{f'(x) = p_x / p_y}. Pass \code{f_prime} if you have it;
otherwise it is approximated numerically and the root is found with
\code{\link[stats:uniroot]{stats::uniroot()}}. Either way the corners are checked: if even the first
unit of \code{x} is not worth its price the consumer buys none, and if the last
affordable unit still is, the consumer buys nothing but \code{x}.

Unlike the other constructors this one has no \code{kind} argument: a function
linear in one input has no sensible reading as a technology, so it is
always a utility function.
}
\examples{
u <- quasilinear(log, f_prime = function(x) 1 / x)
optimal_bundle(u, budget(100, 2, 5))
optimal_bundle(u, budget(500, 2, 5))   # same x: no income effect
}
