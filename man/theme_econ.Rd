% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_econ}
\alias{theme_econ}
\title{An Economist-style ggplot2 theme}
\usage{
theme_econ(
  base_size = 12,
  base_family = "",
  panel = c("blue", "white", "dark"),
  grid = c("y", "x", "both", "none"),
  legend_position = "top"
)
}
\arguments{
\item{base_size}{Base font size in points.}

\item{base_family}{Base font family. \code{""} uses the device default.}

\item{panel}{Panel style: \code{"blue"} (the classic printed panel, the default),
\code{"white"}, or \code{"dark"}.}

\item{grid}{Which major gridlines to draw: \code{"y"} (the default), \code{"x"},
\code{"both"} or \code{"none"}.}

\item{legend_position}{Passed to \code{\link[ggplot2:theme]{ggplot2::theme()}}. Defaults to \code{"top"},
left-justified and without a legend title.}
}
\value{
A ggplot2 theme object.
}
\description{
Reproduces the chart furniture of The Economist: a flat blue-grey panel with
no border, horizontal gridlines only, a solid baseline with ticks on the
x-axis, and left-aligned title, subtitle and caption that hang off the plot
edge rather than the panel.
}
\details{
Two conventions are left to the caller because they depend on the data
rather than the style: put the units in the subtitle and drop the axis
titles with \code{labs(x = NULL, y = NULL)}, and move the y-axis labels to the
right with \code{\link[=scale_y_econ]{scale_y_econ()}}. \code{\link[=labs_econ]{labs_econ()}} does the first for you.

The Economist sets in a proprietary face (Officina Sans / EconSans). This
theme does not ship or assume a font: \code{base_family = ""} means the device
default. Pass a family you actually have installed if you want to get
closer.
}
\examples{
library(ggplot2)
ggplot(economics, aes(date, unemploy / pop * 100)) +
  geom_line(colour = econ_colours("blue"), linewidth = 0.8) +
  scale_y_econ() +
  labs_econ(
    title = "Out of work",
    subtitle = "United States, unemployed as \% of population",
    source = "FRED, Federal Reserve Bank of St Louis"
  ) +
  theme_econ()
}
\seealso{
\code{\link[=scale_colour_econ]{scale_colour_econ()}} for matching colour scales,
\code{\link[=econ_masthead]{econ_masthead()}} for the red block, and \code{\link[=annotate_recessions]{annotate_recessions()}} for
recession bands.
}
