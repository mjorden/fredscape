% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derived.R
\name{geom_consumption_path}
\alias{geom_consumption_path}
\title{Draw the locus of optimal bundles}
\usage{
geom_consumption_path(
  data,
  colour = unname(econ_hex["ink"]),
  linewidth = 0.6,
  size = 2,
  ...
)
}
\arguments{
\item{data}{A data frame with \code{x} and \code{y} columns from one of the path
functions.}

\item{colour}{Path and point colour.}

\item{linewidth}{Path width.}

\item{size}{Point size; \code{0} for no points.}

\item{...}{Passed to \code{\link[ggplot2:geom_path]{ggplot2::geom_path()}}.}
}
\value{
A list of ggplot2 layers.
}
\description{
Adds a \code{\link[=price_consumption_path]{price_consumption_path()}} or \code{\link[=income_consumption_path]{income_consumption_path()}} to the
indifference-curve diagram as a path through the bundles, with a point at
each one. Pair it with \code{\link[=geom_budget]{geom_budget()}} given the matching list of budgets
to show the lines the bundles sit on.
}
\examples{
library(ggplot2)
u <- cobb_douglas(alpha = 0.4)
b <- budget(income = 120, px = 3, py = 4)
incomes <- c(60, 120, 180)
path <- income_consumption_path(u, b, incomes)

ggplot() +
  geom_budget(lapply(incomes, function(i) budget(i, 3, 4))) +
  geom_indifference(u, levels = path$x^0.4 * path$y^0.6, xlim = c(0, 60)) +
  geom_consumption_path(path) +
  coord_cartesian(xlim = c(0, 60), ylim = c(0, 45), expand = FALSE) +
  theme_econ()
}
