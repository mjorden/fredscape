% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-micro.R
\name{geom_micro}
\alias{geom_micro}
\alias{geom_indifference}
\alias{geom_budget}
\alias{geom_optimum}
\title{Chart layers for consumer and producer theory}
\usage{
geom_indifference(
  u,
  levels,
  xlim,
  n = 200L,
  colour = unname(econ_hex["blue"]),
  linewidth = 0.8,
  ...
)

geom_budget(
  b,
  colour = unname(econ_hex["red"]),
  linewidth = 0.8,
  linetype = "solid",
  ...
)

geom_optimum(
  u,
  b,
  colour = unname(econ_hex["ink"]),
  size = 2.5,
  drop_lines = TRUE,
  ...
)
}
\arguments{
\item{u}{A function of \code{x} and \code{y}, typically from \code{\link[=cobb_douglas]{cobb_douglas()}}.}

\item{levels}{Utility (or output) levels, one curve each.}

\item{xlim}{Range of \code{x} over which to draw the curves.}

\item{n}{Number of points per curve.}

\item{colour}{Line or point colour.}

\item{linewidth}{Line width.}

\item{...}{Passed to the underlying geom.}

\item{b}{A \code{\link[=budget]{budget()}}.}

\item{linetype}{Line type; the drop lines in \code{geom_optimum()} are always
dashed.}

\item{size}{Point size.}

\item{drop_lines}{Draw the dashed lines from the optimum to each axis?}
}
\value{
A ggplot2 layer, or a list of layers, that can be added to a plot
with \code{+}.
}
\description{
Layers that put \code{\link[=indifference_curve]{indifference_curve()}}, \code{\link[=budget]{budget()}} and \code{\link[=optimal_bundle]{optimal_bundle()}}
onto a ggplot without constructing the data by hand. None of them inherit
aesthetics from the plot, so they can be added to an empty \code{ggplot()} or
combined with other data.
}
\details{
\itemize{
\item \code{geom_indifference()} draws one path per \code{level}.
\item \code{geom_budget()} draws the budget (or isocost) line between its two
intercepts.
\item \code{geom_optimum()} marks the chosen bundle, with dashed lines dropping to
each axis.
}
}
\examples{
library(ggplot2)
u <- cobb_douglas(alpha = 0.4)
b <- budget(income = 120, px = 3, py = 4)

ggplot() +
  geom_indifference(u, levels = c(8, 12, 16), xlim = c(0, 45)) +
  geom_budget(b) +
  geom_optimum(u, b) +
  coord_cartesian(xlim = c(0, 45), ylim = c(0, 35), expand = FALSE) +
  theme_econ()
}
\seealso{
\code{\link[=plot_consumer_choice]{plot_consumer_choice()}} for the whole chart in one call.
}
