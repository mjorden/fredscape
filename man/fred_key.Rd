% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/key.R
\name{fred_key}
\alias{fred_key}
\alias{fred_has_key}
\alias{fred_set_key}
\title{Manage the FRED API key}
\usage{
fred_key()

fred_has_key()

fred_set_key(key)
}
\arguments{
\item{key}{A 32-character lower-case alphanumeric FRED API key.}
}
\value{
\code{fred_key()} returns the key as a string, and errors if none is set.
\code{fred_has_key()} returns \code{TRUE} or \code{FALSE} and never errors.
\code{fred_set_key()} invisibly returns the previous value of the environment
variable, so it can be restored.
}
\description{
FRED requires a free API key for every request. \code{fredscape} reads it from the
\code{FRED_API_KEY} environment variable so that the key never has to appear in a
script, a git history, or an error message.
}
\details{
Request a key at \url{https://fredaccount.stlouisfed.org/apikeys}, then either
add \verb{FRED_API_KEY=<your key>} to your user-level \code{.Renviron}
(\code{usethis::edit_r_environ()}) or call \code{\link[=fred_set_key]{fred_set_key()}} for the current
session.
}
\examples{
fred_has_key()

# Set a key for this session only:
old <- fred_set_key(strrep("a", 32))
fred_has_key()
Sys.setenv(FRED_API_KEY = old)
}
