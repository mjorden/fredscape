% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/market.R
\name{cost}
\alias{cost}
\alias{quadratic_cost}
\alias{production_cost}
\title{A firm's cost function}
\usage{
quadratic_cost(fixed = 0, a = 0, b = 0)

production_cost(f, w, r, fixed = 0)
}
\arguments{
\item{fixed}{Fixed cost. Non-negative.}

\item{a}{Linear coefficient: marginal cost at zero output. Non-negative.}

\item{b}{Quadratic coefficient. Non-negative. (The textbook letter, not a
\code{\link[=budget]{budget()}}; see \code{?fredscape-extending} for the naming conventions.)}

\item{f}{A production function of \code{x} and \code{y}.}

\item{w, r}{Input prices.}
}
\value{
An object of class \code{cost} (and \code{quadratic_cost} or
\code{production_cost}).
}
\description{
\code{quadratic_cost()} is the textbook \eqn{C(q) = F + a q + b q^2}: constant
marginal cost when \code{b = 0}, rising marginal cost otherwise, and a U-shaped
average cost whenever both \code{fixed} and \code{b} are positive.
\code{production_cost()} derives the cost function from a production function
and input prices through \code{\link[=expenditure]{expenditure()}}, so the market module can sit
directly on the producer-theory one.
}
\examples{
cst <- quadratic_cost(fixed = 100, a = 20, b = 0.5)
cst
marginal_cost(cst, 10)
min_average_cost(cst)

pc <- production_cost(cobb_douglas(0.3, 0.5, kind = "production"), w = 20, r = 30)
total_cost(pc, 10)
}
\seealso{
\code{\link[=total_cost]{total_cost()}} and friends; \code{\link[=monopoly]{monopoly()}}, \code{\link[=cournot]{cournot()}},
\code{\link[=perfect_competition]{perfect_competition()}}.
}
