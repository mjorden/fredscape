% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/micro.R, R/utilities.R
\name{optimal_bundle}
\alias{optimal_bundle}
\alias{optimal_bundle.cobb_douglas}
\alias{optimal_bundle.default}
\alias{optimal_bundle.ces}
\alias{optimal_bundle.leontief}
\alias{optimal_bundle.perfect_substitutes}
\alias{optimal_bundle.quasilinear}
\title{The bundle a consumer (or producer) chooses}
\usage{
optimal_bundle(u, b, ...)

\method{optimal_bundle}{cobb_douglas}(u, b, ...)

\method{optimal_bundle}{default}(u, b, ...)

\method{optimal_bundle}{ces}(u, b, ...)

\method{optimal_bundle}{leontief}(u, b, ...)

\method{optimal_bundle}{perfect_substitutes}(u, b, ...)

\method{optimal_bundle}{quasilinear}(u, b, ...)
}
\arguments{
\item{u}{A function of \code{x} and \code{y}.}

\item{b}{A \code{\link[=budget]{budget()}}.}

\item{...}{Passed on to methods.}
}
\value{
A one-row data frame with \code{x}, \code{y} and \code{utility} (the value of \code{u}
at the optimum).
}
\description{
Maximises \code{u} subject to the budget constraint \code{b}. A Cobb-Douglas
consumer spends the fixed share \eqn{\alpha / (\alpha + \beta)} of income
on \code{x}, which gives the closed form
\eqn{x^* = \frac{\alpha}{\alpha + \beta} \frac{I}{p_x}}. Any other
function is maximised numerically along the budget line with
\code{\link[stats:optimize]{stats::optimize()}}, which finds the global optimum only if \code{u} is
quasi-concave -- true for the usual textbook cases.
}
\examples{
u <- cobb_douglas(alpha = 0.3)
b <- budget(income = 100, px = 2, py = 5)
optimal_bundle(u, b)

# Spends 30\% of income on x regardless of prices:
optimal_bundle(u, budget(100, 4, 5))$x * 4
}
