% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unit-root.R
\name{adf_test}
\alias{adf_test}
\title{Augmented Dickey-Fuller test for a unit root}
\usage{
adf_test(x, lags = NULL, type = c("drift", "trend", "none"), max_lags = NULL)
}
\arguments{
\item{x}{A numeric vector, or a single-series data frame with a \code{value}
column.}

\item{lags}{Number of lagged differences \code{p}. With \code{NULL} the lag length
is chosen by AIC over \code{0:max_lags}.}

\item{type}{Deterministic terms: \code{"drift"}, \code{"trend"} or \code{"none"}.}

\item{max_lags}{Upper bound for the AIC search. Defaults to Schwert's
\eqn{\lfloor 12 (T/100)^{1/4} \rfloor}.}
}
\value{
An object of class \code{adf_test}: a list with \code{statistic},
\code{critical} (named vector at 1, 5 and 10 percent), \code{reject} (logical, per
level), \code{lags}, \code{type}, \code{nobs} and the fitted \code{regression}. No p-value
is reported; compare the statistic with the critical values, which is
what the printer does.
}
\description{
Estimates \eqn{\Delta y_t = \alpha + \beta t + \gamma y_{t-1} +
\sum_{i=1}^{p} \phi_i \Delta y_{t-i} + \varepsilon_t} and tests
\eqn{\gamma = 0} (a unit root) against \eqn{\gamma < 0} (stationarity)
with the t statistic on \eqn{\gamma}. Under the null that statistic does
not have a t distribution; critical values come from MacKinnon's (2010)
response surfaces, which adjust for sample size.
}
\details{
Choose \code{type} by what the series looks like under the alternative:
\code{"drift"} (the default) for a series that fluctuates around a non-zero
level, \code{"trend"} for one that grows, \code{"none"} only for a series known to
be centred on zero. Including deterministic terms the data do not need
costs power; leaving out terms it does need makes the test invalid.
}
\examples{
set.seed(1)
rw <- cumsum(rnorm(200))         # a random walk: do not reject
adf_test(rw)

ar <- as.numeric(stats::arima.sim(list(ar = 0.5), 200))   # stationary: reject
adf_test(ar)
}
\references{
MacKinnon, J. G. (2010). Critical values for cointegration tests. Queen's
Economics Department Working Paper 1227.

Said, S. E. and Dickey, D. A. (1984). Testing for unit roots in
autoregressive-moving average models of unknown order. \emph{Biometrika} 71(3).
}
