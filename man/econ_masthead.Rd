% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/masthead.R
\name{econ_masthead}
\alias{econ_masthead}
\title{Add the red masthead block above a chart}
\usage{
econ_masthead(
  plot,
  colour = unname(econ_hex["red"]),
  width = grid::unit(0.55, "cm"),
  height = grid::unit(0.13, "cm"),
  gap = grid::unit(0.3, "cm")
)
}
\arguments{
\item{plot}{A ggplot object, or a gtable from a previous
\code{\link[ggplot2:ggplotGrob]{ggplot2::ggplotGrob()}} call.}

\item{colour}{Block colour. Defaults to the Economist red.}

\item{width, height}{Block dimensions, as \code{\link[grid:unit]{grid::unit()}} objects.}

\item{gap}{Space between the block and whatever is below it.}
}
\value{
An object of class \code{econ_plot}, which inherits from \code{gtable}.
}
\description{
The Economist prefixes every chart with a small red rectangle at the top
left, above the title. That block is not a plot layer -- it sits outside the
plotting area entirely -- so it cannot be added with \code{+}. This function
converts the plot to a grob table and inserts the block as a new row aligned
with the title.
}
\details{
The result is a grid object, not a ggplot, so it is the last thing you add.
It prints normally and \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} accepts it, but you cannot add
further ggplot layers to it afterwards.
}
\examples{
library(ggplot2)
p <- ggplot(economics, aes(date, psavert)) +
  geom_line(colour = econ_colours("blue"), linewidth = 0.7) +
  scale_y_econ() +
  labs_econ(
    title = "Saving grace",
    subtitle = "United States, personal saving rate, \% of disposable income",
    source = "FRED, Federal Reserve Bank of St Louis"
  ) +
  theme_econ()

econ_masthead(p)
}
