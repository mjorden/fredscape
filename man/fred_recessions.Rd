% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recessions.R
\name{fred_recessions}
\alias{fred_recessions}
\title{Fetch recession dates live from FRED}
\usage{
fred_recessions(key = fred_key())
}
\arguments{
\item{key}{A FRED API key. Defaults to \code{\link[=fred_key]{fred_key()}}.}
}
\value{
A data frame with \code{peak} and \code{trough} \code{Date} columns, in the same
shape \code{\link[=annotate_recessions]{annotate_recessions()}} expects.
}
\description{
Downloads \code{USREC}, the NBER-based recession indicator (1 during a
contraction, 0 otherwise), and collapses each run of ones into a
peak/trough interval. Use this instead of the bundled \link{nber_recessions} when
you need the current dating, including any recession called since this
package was last released.
}
\examples{
\dontshow{if (fredscape::fred_has_key()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
tail(fred_recessions(), 3)
\dontshow{\}) # examplesIf}
}
