% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrimination.R
\name{aggregate_demand}
\alias{aggregate_demand}
\title{Horizontal summation of demand curves}
\usage{
aggregate_demand(demands)
}
\arguments{
\item{demands}{A list of demand objects.}
}
\value{
A \code{general_demand} object whose \code{q_max} is the sum of the parts'.
}
\description{
The market demand faced by a seller who cannot tell buyer groups apart:
at every price, the sum of the quantities each group demands. The inverse
is found numerically, so the result is a \code{\link[=demand_fn]{demand_fn()}} and works anywhere
a demand object does -- most usefully as the uniform-price benchmark for
\code{\link[=third_degree]{third_degree()}}.
}
\examples{
students <- linear_demand(intercept = 60, slope = 0.5)
others <- linear_demand(intercept = 100, slope = 1)
both <- aggregate_demand(list(students, others))
quantity_at(both, 40)   # 40 + 60
price_at(both, 100)
}
