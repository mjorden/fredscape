% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/price-change.R
\name{plot_price_change}
\alias{plot_price_change}
\title{Draw the substitution and income effects of a price change}
\usage{
plot_price_change(
  u,
  b,
  new_px = NULL,
  new_py = NULL,
  method = c("hicks", "slutsky"),
  goods = c("Good x", "Good y"),
  title = NULL,
  subtitle = NULL,
  source = NULL,
  xlim = NULL,
  ylim = NULL,
  panel = "blue"
)
}
\arguments{
\item{u}{A function of \code{x} and \code{y}.}

\item{b}{The original \code{\link[=budget]{budget()}}.}

\item{new_px, new_py}{The new price. Supply exactly one.}

\item{method}{\code{"hicks"} or \code{"slutsky"}.}

\item{goods}{Axis labels for \code{x} and \code{y}.}

\item{title, subtitle, source}{Passed to \code{\link[=labs_econ]{labs_econ()}}; sensible defaults
are filled in.}

\item{xlim, ylim}{Panel limits. Default to a little beyond the widest
budget line.}

\item{panel}{Passed to \code{\link[=theme_econ]{theme_econ()}}.}
}
\value{
A ggplot object. Add \code{\link[=econ_masthead]{econ_masthead()}} last if you want the block.
}
\description{
The diagram behind \code{\link[=price_change]{price_change()}}: the original and final budget lines,
the dashed compensated line, the indifference curves through the bundles,
the three bundles themselves, and brackets along the axis of the good whose
price changed showing how far the substitution and income effects each
move the quantity.
}
\examples{
u <- cobb_douglas(alpha = 0.4)
b <- budget(income = 120, px = 3, py = 4)
plot_price_change(u, b, new_px = 6)
plot_price_change(u, b, new_px = 6, method = "slutsky")
}
