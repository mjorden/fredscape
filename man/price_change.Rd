% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/price-change.R
\name{price_change}
\alias{price_change}
\alias{slutsky}
\alias{hicks}
\title{Decompose a price change into substitution and income effects}
\usage{
price_change(
  u,
  b,
  new_px = NULL,
  new_py = NULL,
  method = c("hicks", "slutsky")
)

slutsky(u, b, new_px = NULL, new_py = NULL)

hicks(u, b, new_px = NULL, new_py = NULL)
}
\arguments{
\item{u}{A function of \code{x} and \code{y}, typically from \code{\link[=cobb_douglas]{cobb_douglas()}} or one
of its siblings.}

\item{b}{The original \code{\link[=budget]{budget()}}.}

\item{new_px, new_py}{The new price. Supply exactly one.}

\item{method}{\code{"hicks"} (the default) or \code{"slutsky"}.}
}
\value{
An object of class \code{price_change}: a list with
\itemize{
\item \code{bundles} -- a data frame with one row each for the \code{original},
\code{compensated} and \code{final} bundles: \code{stage}, \code{x}, \code{y}, \code{utility},
\code{income}.
\item \code{effects} -- a data frame with rows \code{substitution}, \code{income} and
\code{total}, and columns \code{dx} and \code{dy}.
\item \code{budgets} -- the three \code{\link[=budget]{budget()}} objects, in the same order.
\item \code{method}, \code{good} (the good whose price changed), \code{old_price},
\code{new_price}.
}
}
\description{
When the price of one good changes, the consumer moves from the original
bundle to a new one. The textbook splits that move in two by imagining an
intermediate, \emph{compensated} budget at the new prices:
}
\details{
\itemize{
\item \strong{Hicks} compensation gives the consumer just enough income to reach
the original utility at the new prices. The compensated bundle sits on
the original indifference curve.
\item \strong{Slutsky} compensation gives just enough income to afford the original
bundle at the new prices. The compensated budget line passes through the
original bundle.
}

The move from the original bundle to the compensated one is the
\emph{substitution effect} (a pure response to relative prices); the move from
the compensated bundle to the final one is the \emph{income effect}.

\code{slutsky()} and \code{hicks()} are shorthands for \code{price_change()} with
\code{method} fixed.
}
\examples{
u <- cobb_douglas(alpha = 0.4)
b <- budget(income = 120, px = 3, py = 4)

pc <- price_change(u, b, new_px = 6)
pc
pc$effects

# Quasi-linear preferences: the income effect on x vanishes
q <- quasilinear(log, f_prime = function(x) 1 / x)
slutsky(q, b, new_px = 6)$effects
}
\seealso{
\code{\link[=plot_price_change]{plot_price_change()}} to draw it.
}
