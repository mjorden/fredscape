% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrimination.R
\name{plot_two_part_tariff}
\alias{plot_two_part_tariff}
\title{Draw a two-part tariff}
\usage{
plot_two_part_tariff(
  tariff,
  type = NULL,
  title = NULL,
  subtitle = NULL,
  source = NULL,
  panel = "blue"
)
}
\arguments{
\item{tariff}{A \code{two_part_tariff} from \code{\link[=two_part_tariff]{two_part_tariff()}}.}

\item{type}{Which served type to draw; defaults to the one that sets the
fee.}

\item{title, subtitle, source}{Passed to \code{\link[=labs_econ]{labs_econ()}}; sensible defaults
are filled in.}

\item{panel}{Passed to \code{\link[=theme_econ]{theme_econ()}}.}
}
\value{
A ggplot object. Add \code{\link[=econ_masthead]{econ_masthead()}} last if you want the block.
}
\description{
One served consumer's demand curve, the per-unit price, the quantity
bought, and the fee as the shaded surplus above the price. With several
types the type whose surplus pins the fee is drawn, and any surplus a
higher type keeps is left unshaded.
}
\examples{
d <- linear_demand(intercept = 100, slope = 1)
plot_two_part_tariff(two_part_tariff(d, quadratic_cost(a = 20)))
}
