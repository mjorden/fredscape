% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters.R
\name{trend_cycle}
\alias{trend_cycle}
\alias{hp_filter}
\alias{hamilton_filter}
\title{Trend-cycle decompositions}
\usage{
hp_filter(x, lambda = NULL, frequency = c("quarterly", "annual", "monthly"))

hamilton_filter(x, h = 8L, p = 4L)
}
\arguments{
\item{x}{A numeric vector, or a data frame from \code{\link[=fred_series]{fred_series()}} with
\code{date} and \code{value} columns (a single series).}

\item{lambda}{Smoothing parameter. Overrides \code{frequency}.}

\item{frequency}{\code{"annual"}, \code{"quarterly"} or \code{"monthly"}, used to pick
\code{lambda} when it is not given.}

\item{h}{Forecast horizon.}

\item{p}{Number of lags in the forecasting regression (including the
current value).}
}
\value{
An object of class \code{trend_cycle}: a data frame with columns
\code{date} (if \code{x} had one), \code{value}, \code{trend} and \code{cycle}, plus attributes
\code{method} and the tuning parameters. \code{\link[=plot_trend_cycle]{plot_trend_cycle()}} draws it.
}
\description{
Two ways of splitting a series into a slow-moving trend and the cycle
around it.
}
\details{
\code{hp_filter()} is Hodrick and Prescott (1997): the trend minimises
\eqn{\sum (y_t - \tau_t)^2 + \lambda \sum (\Delta^2 \tau_t)^2}, a
pentadiagonal linear system solved directly. The smoothing parameter is
usually set by data frequency; the defaults are the conventional
100 / 1600 / 14400 for annual / quarterly / monthly data. (Ravn and Uhlig
2002 argue for 6.25 / 1600 / 129600; pass \code{lambda} to use them.)

\code{hamilton_filter()} is Hamilton (2018), who argues the HP filter invents
spurious cycles: regress \eqn{y_{t+h}} on the current value and \code{p - 1}
lags, and call the residual the cycle -- the part of the series \code{h}
periods ahead that could not be forecast from its recent past. The first
\code{h + p - 1} observations have no cycle value. Defaults \verb{h = 8, p = 4} are
the paper's quarterly recommendation; use \verb{h = 24, p = 12} for monthly
data.
}
\examples{
econ <- ggplot2::economics
u <- data.frame(date = econ$date, value = 100 * econ$unemploy / econ$pop)

hp <- hp_filter(u, frequency = "monthly")
head(hp)

hm <- hamilton_filter(u, h = 24, p = 12)
range(hm$cycle, na.rm = TRUE)
}
\references{
Hodrick, R. J. and Prescott, E. C. (1997). Postwar U.S. business cycles:
an empirical investigation. \emph{Journal of Money, Credit and Banking} 29(1).

Hamilton, J. D. (2018). Why you should never use the Hodrick-Prescott
filter. \emph{Review of Economics and Statistics} 100(5).

Ravn, M. O. and Uhlig, H. (2002). On adjusting the Hodrick-Prescott filter
for the frequency of observations. \emph{Review of Economics and Statistics}
84(2).
}
