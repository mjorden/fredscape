% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters.R
\name{plot_trend_cycle}
\alias{plot_trend_cycle}
\title{Draw a trend-cycle decomposition}
\usage{
plot_trend_cycle(
  tc,
  title = NULL,
  subtitle = NULL,
  source = NULL,
  recessions = TRUE,
  panel = "blue"
)
}
\arguments{
\item{tc}{A \code{trend_cycle} from \code{\link[=hp_filter]{hp_filter()}} or \code{\link[=hamilton_filter]{hamilton_filter()}}.}

\item{title, subtitle, source}{Passed to \code{\link[=labs_econ]{labs_econ()}}; sensible defaults
are filled in.}

\item{recessions}{Shade NBER recessions? Only possible when \code{tc} has a
\code{date} column.}

\item{panel}{Passed to \code{\link[=theme_econ]{theme_econ()}}.}
}
\value{
A ggplot object. Add \code{\link[=econ_masthead]{econ_masthead()}} last if you want the block.
}
\description{
Two panels in the house style: the series with its trend, and the cycle
below it around zero. With dates present, NBER recessions are shaded.
}
\examples{
econ <- ggplot2::economics
u <- data.frame(date = econ$date, value = 100 * econ$unemploy / econ$pop)
plot_trend_cycle(hp_filter(u, frequency = "monthly"),
                 title = "Unemployment and its trend")
}
