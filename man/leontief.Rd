% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{leontief}
\alias{leontief}
\title{Perfect complements (Leontief) functions}
\usage{
leontief(a = 1, b = 1, A = 1, kind = c("utility", "production"))
}
\arguments{
\item{a, b}{Units of \code{x} and \code{y} needed per unit of output. Positive.
(\code{b} here is the textbook coefficient, not a \code{\link[=budget]{budget()}}.)}

\item{A}{Scale factor. Positive.}

\item{kind}{\code{"utility"} or \code{"production"}.}
}
\value{
A function of \code{x} and \code{y} of class \code{leontief}, with \code{a}, \code{b}, \code{A}
and \code{kind} as attributes.
}
\description{
Builds \eqn{f(x, y) = A \min(x / a, y / b)}: the goods are only useful in
the fixed proportion \eqn{a : b}, so the contours are L-shaped with the
kink on the ray \eqn{x / a = y / b}. The consumer always chooses the kink,
whatever the prices.
}
\details{
The MRS is not a single number here. Below the ray (\code{x / a < y / b}) \code{x} is
the scarce good and the consumer would give up any amount of \code{y} for it, so
\code{\link[=mrs]{mrs()}} returns \code{Inf}; above the ray it returns \code{0}; on the ray itself the
curve has a corner and the MRS is undefined, returned as \code{NA}.
}
\examples{
u <- leontief(a = 1, b = 2)   # one x for every two y
u(3, 6)
optimal_bundle(u, budget(100, 2, 5))
mrs(u, c(1, 3, 5), 6)          # Inf below the ray, NA on it, 0 above
}
