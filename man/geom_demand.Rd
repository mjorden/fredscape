% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derived.R
\name{geom_demand}
\alias{geom_demand}
\alias{geom_engel}
\title{Draw a demand or Engel curve}
\usage{
geom_demand(data, colour = unname(econ_hex["blue"]), linewidth = 0.8, ...)

geom_engel(data, colour = unname(econ_hex["blue"]), linewidth = 0.8, ...)
}
\arguments{
\item{data}{A data frame from \code{\link[=demand_curve]{demand_curve()}} or \code{\link[=engel_curve]{engel_curve()}}.}

\item{colour}{Line colour.}

\item{linewidth}{Line width.}

\item{...}{Passed to \code{\link[ggplot2:geom_path]{ggplot2::geom_path()}}.}
}
\value{
A ggplot2 layer.
}
\description{
\code{geom_demand()} draws the output of \code{\link[=demand_curve]{demand_curve()}} the way every textbook
does: quantity along the x-axis, price up the y-axis. \code{geom_engel()} draws
\code{\link[=engel_curve]{engel_curve()}} with income along the x-axis.
}
\examples{
library(ggplot2)
u <- cobb_douglas(alpha = 0.4)
b <- budget(income = 120, px = 3, py = 4)
d <- demand_curve(u, b, prices = seq(0.5, 10, by = 0.25))

ggplot() +
  geom_demand(d) +
  labs_econ(title = "Demand for x", subtitle = "Price against quantity") +
  labs(x = "Quantity", y = "Price") +
  theme_econ()
}
