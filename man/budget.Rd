% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/micro.R
\name{budget}
\alias{budget}
\title{A budget constraint}
\usage{
budget(income, px, py)
}
\arguments{
\item{income}{Income, or total outlay. Positive.}

\item{px, py}{Prices of \code{x} and \code{y}. Positive.}
}
\value{
A list of class \code{budget} with the inputs plus the derived
\code{x_max} (\code{income / px}), \code{y_max} (\code{income / py}) and \code{slope} (\code{-px / py}).
}
\description{
Describes the set of bundles affordable at income \code{income} when the two
goods cost \code{px} and \code{py}: every \eqn{(x, y)} with
\eqn{p_x x + p_y y \le I}. For a producer the same object is an isocost
line, with \code{income} read as total outlay and the prices as input prices.
}
\examples{
b <- budget(income = 100, px = 2, py = 5)
b
b$x_max
}
\seealso{
\code{\link[=budget_line]{budget_line()}} to get plottable coordinates, \code{\link[=optimal_bundle]{optimal_bundle()}}
to solve against a utility function.
}
