% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-market.R
\name{plot_market}
\alias{plot_market}
\title{Draw a market outcome}
\usage{
plot_market(
  outcome,
  q_max = NULL,
  shade = c("cs", "ps", "dwl"),
  title = NULL,
  subtitle = NULL,
  source = NULL,
  panel = "blue"
)
}
\arguments{
\item{outcome}{A \code{market_outcome} from \code{\link[=monopoly]{monopoly()}}, \code{\link[=cournot]{cournot()}},
\code{\link[=perfect_competition]{perfect_competition()}} or \code{\link[=first_degree]{first_degree()}}. For first-degree
discrimination the whole area between demand and marginal cost is
shaded as producer surplus, since that is who gets it.}

\item{q_max}{Right-hand limit of the quantity axis. Defaults to the
demand curve's \code{q_max}.}

\item{shade}{Which areas to shade: any of \code{"cs"}, \code{"ps"}, \code{"dwl"}.}

\item{title, subtitle, source}{Passed to \code{\link[=labs_econ]{labs_econ()}}; sensible defaults
are filled in.}

\item{panel}{Passed to \code{\link[=theme_econ]{theme_econ()}}.}
}
\value{
A ggplot object. Add \code{\link[=econ_masthead]{econ_masthead()}} last if you want the block.
}
\description{
The demand curve, the industry marginal-cost curve (marginal cost at each
firm's share of the quantity), marginal revenue where it matters, the
outcome, and the welfare areas: consumer surplus above the price, producer
surplus between the price and marginal cost, and the deadweight-loss
triangle between the outcome and the efficient quantity.
}
\examples{
d <- linear_demand(intercept = 100, slope = 1)
plot_market(monopoly(d, quadratic_cost(a = 20)))
plot_market(cournot(d, quadratic_cost(a = 20), n = 3))
plot_market(perfect_competition(d, quadratic_cost(fixed = 100, a = 20, b = 1)))
}
