% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/micro.R
\name{cobb_douglas}
\alias{cobb_douglas}
\title{Cobb-Douglas utility and production functions}
\usage{
cobb_douglas(alpha, beta = 1 - alpha, A = 1, kind = c("utility", "production"))
}
\arguments{
\item{alpha}{Exponent on \code{x}. Must be positive.}

\item{beta}{Exponent on \code{y}. Defaults to \code{1 - alpha}, which gives a
homogeneous-of-degree-one function; any positive value is allowed.}

\item{A}{Scale factor (total factor productivity, for a production
function). Must be positive.}

\item{kind}{\code{"utility"} or \code{"production"}.}
}
\value{
A function of two arguments \code{x} and \code{y}, of class \code{cobb_douglas},
carrying \code{alpha}, \code{beta}, \code{A} and \code{kind} as attributes.
}
\description{
Builds the function \eqn{f(x, y) = A x^{\alpha} y^{\beta}} as a callable R
function that also remembers its parameters, so the closed-form results
(indifference curves, demand, marginal rate of substitution) can be read
straight off it rather than re-derived numerically.
}
\details{
The same object serves as a utility function over two goods or a production
function over two inputs; the maths is identical, only the vocabulary
changes (indifference curve vs isoquant, budget line vs isocost). \code{kind}
records which reading is intended and drives the labels that
\code{\link[=plot_consumer_choice]{plot_consumer_choice()}} and \code{\link[=print]{print()}} use.
}
\examples{
u <- cobb_douglas(alpha = 0.3)
u
u(x = 4, y = 9)

f <- cobb_douglas(alpha = 0.6, beta = 0.6, A = 2, kind = "production")
f
}
\seealso{
\code{\link[=indifference_curve]{indifference_curve()}}, \code{\link[=optimal_bundle]{optimal_bundle()}}, \code{\link[=mrs]{mrs()}}, \code{\link[=budget]{budget()}}.
}
