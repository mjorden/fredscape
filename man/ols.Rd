% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ols.R
\name{ols}
\alias{ols}
\title{Ordinary least squares with robust standard errors}
\usage{
ols(formula, data, se = c("classical", "hc1", "hac"), lags = NULL, ...)
}
\arguments{
\item{formula}{A model formula, as for \code{\link[stats:lm]{stats::lm()}}.}

\item{data}{A data frame.}

\item{se}{Which standard errors: \code{"classical"}, \code{"hc1"} or \code{"hac"}.}

\item{lags}{Bandwidth for \code{"hac"}. Ignored otherwise.}

\item{...}{Passed to \code{\link[stats:lm]{stats::lm()}}, e.g. \code{subset} or \code{weights}.}
}
\value{
An object of class \code{econ_fit}: a list with \code{fit} (the \code{lm}),
\code{vcov}, \code{se_type}, \code{lags}, \code{nobs} and \code{df}. \code{\link[=coef_table]{coef_table()}} gives the
coefficient table; \code{\link[=plot_coefficients]{plot_coefficients()}} draws it.
}
\description{
\code{\link[stats:lm]{stats::lm()}} underneath, with the covariance matrix replaced by one that
survives the two things macro data always has: heteroskedasticity and
serial correlation.
}
\details{
\itemize{
\item \code{"classical"} -- the usual \eqn{\sigma^2 (X'X)^{-1}}.
\item \code{"hc1"} -- White's heteroskedasticity-consistent estimator with the
\eqn{n / (n - k)} degrees-of-freedom correction (the default in Stata's
\code{robust} option).
\item \code{"hac"} -- Newey and West (1987): Bartlett-weighted autocovariances of
the score up to \code{lags}. With \code{lags = NULL} the bandwidth is
\eqn{\lfloor 4 (n/100)^{2/9} \rfloor}, the rule of thumb most software
uses. No small-sample adjustment is applied.
}

Both robust estimators are computed directly from the design matrix and
residuals -- a dozen lines of linear algebra -- rather than through a
dependency, and the tests pin them against hand-computed sandwich matrices.
}
\examples{
econ <- ggplot2::economics
econ$unemp_rate <- 100 * econ$unemploy / econ$pop

# Saving and unemployment, with serial correlation handled
fit <- ols(psavert ~ unemp_rate, econ, se = "hac")
fit
coef_table(fit)
}
