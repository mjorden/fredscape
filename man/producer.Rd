% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/producer.R
\name{producer}
\alias{producer}
\alias{expansion_path}
\alias{conditional_demand}
\alias{cost_curves}
\title{Producer theory: expansion paths and cost curves}
\usage{
expansion_path(f, w, r, outlays)

conditional_demand(f, w, r, q)

cost_curves(f, w, r, q, fixed = 0)
}
\arguments{
\item{f}{A production function of \code{x} and \code{y}, e.g.
\code{cobb_douglas(0.3, 0.5, kind = "production")}.}

\item{w, r}{Input prices for \code{x} and \code{y} (wage and rental rate, by
convention).}

\item{outlays}{Vector of total outlays.}

\item{q}{Vector of output levels. Positive.}

\item{fixed}{A fixed cost added to total cost. It does not affect marginal
cost, but gives average cost its textbook U shape.}
}
\value{
A data frame:
\itemize{
\item \code{expansion_path()}: \code{outlay}, \code{x}, \code{y}, \code{output}.
\item \code{conditional_demand()}: \code{output}, \code{x}, \code{y}, \code{cost}.
\item \code{cost_curves()}: \code{output}, \code{total}, \code{average}, \code{marginal}.
}
}
\description{
The consumer's problem and the producer's cost-minimisation problem are the
same mathematics with different names: a production function is a utility
function over inputs, an isocost line is a budget line with input prices,
and the expenditure function is the total-cost function. These helpers put
the producer's vocabulary on the machinery the rest of the package already
has.
}
\details{
\itemize{
\item \code{expansion_path()} -- the cost-minimising input bundle at each level of
outlay, i.e. the income-consumption path read as a producer.
\item \code{conditional_demand()} -- the cheapest input bundle that produces each
output level, and what it costs.
\item \code{cost_curves()} -- total, average and marginal cost at each output level.
Marginal cost is closed-form for Cobb-Douglas (\eqn{C(q) \propto
  q^{1/(\alpha+\beta)}}, so \eqn{MC = C / ((\alpha + \beta) q)}) and a
numerical derivative of \code{\link[=expenditure]{expenditure()}} otherwise; for the constant-
returns constructors that derivative is exact.
}
}
\examples{
f <- cobb_douglas(0.3, 0.5, A = 2, kind = "production")
f   # decreasing returns to scale

expansion_path(f, w = 20, r = 30, outlays = c(300, 600, 900))
conditional_demand(f, w = 20, r = 30, q = c(5, 10, 20))
cost_curves(f, w = 20, r = 30, q = c(5, 10, 20), fixed = 100)
}
