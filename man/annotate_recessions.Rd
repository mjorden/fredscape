% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recessions.R
\name{annotate_recessions}
\alias{annotate_recessions}
\title{Shade recession bands behind a time series}
\usage{
annotate_recessions(
  data = nber_recessions,
  from = NULL,
  to = NULL,
  fill = "#8FA5B0",
  alpha = 0.35
)
}
\arguments{
\item{data}{A data frame of recession intervals with \code{peak} and \code{trough}
date columns. Defaults to \link{nber_recessions}; pass the result of
\code{\link[=fred_recessions]{fred_recessions()}} for a live copy.}

\item{from, to}{Optional window. Recessions that do not overlap it are
dropped, which keeps the x-axis from stretching back to 1857 when the
series starts in 1990.}

\item{fill}{Band colour.}

\item{alpha}{Band opacity.}
}
\value{
A ggplot2 annotation layer, or a zero-row layer if nothing overlaps
the window.
}
\description{
Returns a rectangle annotation spanning the full height of the panel for
each recession, to be added before the data layers so the shading sits
underneath them.
}
\examples{
library(ggplot2)
ggplot(economics, aes(date, uempmed)) +
  annotate_recessions(from = min(economics$date), to = max(economics$date)) +
  geom_line(colour = econ_colours("blue"), linewidth = 0.7) +
  scale_x_econ_date() +
  scale_y_econ() +
  labs_econ(
    title = "The long wait",
    subtitle = "United States, median duration of unemployment, weeks",
    note = "Shaded areas are NBER-dated recessions",
    source = "FRED, Federal Reserve Bank of St Louis"
  ) +
  theme_econ()
}
