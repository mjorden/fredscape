% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/micro.R
\name{indifference_curve}
\alias{indifference_curve}
\alias{indifference_curve.cobb_douglas}
\alias{indifference_curve.default}
\title{Indifference curves and isoquants}
\usage{
indifference_curve(u, level, x, ...)

\method{indifference_curve}{cobb_douglas}(u, level, x, ...)

\method{indifference_curve}{default}(u, level, x, ..., y_range = c(1e-09, 1e+09))
}
\arguments{
\item{u}{A function of \code{x} and \code{y}, typically from \code{\link[=cobb_douglas]{cobb_douglas()}}.}

\item{level}{One or more utility (or output) levels.}

\item{x}{Numeric vector of \code{x} values at which to evaluate the curve.}

\item{...}{Passed on to methods.}

\item{y_range}{Search interval for the numeric solver. Defaults to a wide
positive range; narrow it if the function is only well-behaved on part of
the plane.}
}
\value{
A data frame with columns \code{x}, \code{y} and \code{level}, one row per \code{x}
per level, stacked in \code{level} order. \code{y} is \code{NA} where no solution
exists: for the numeric method, where the level is unreachable within
\code{y_range}; for Cobb-Douglas, at \code{x <= 0} or \code{level <= 0}, where the
closed form is undefined. The two methods agree wherever a level is
unattainable and at \code{x = 0} for any positive level, so a caller can
filter on \code{is.na(y)} without knowing which one ran. (At \code{level = 0}
exactly, a Cobb-Douglas contour is the pair of axes; the closed form
reports \code{NA} rather than pretend that is a curve.)
}
\description{
For each \code{level}, finds the \code{y} that gives \eqn{u(x, y) = } \code{level} at
every \code{x}, i.e. the contour of the function. Cobb-Douglas functions use
the closed form \eqn{y = (U / (A x^{\alpha}))^{1/\beta}}; any other
function of two arguments is solved numerically by bisection, which
requires it to be non-decreasing in \code{y} ("more is better"). Where a
contour is flat in \code{y} -- perfect complements, for instance -- the
smallest \code{y} that attains the level is returned, which is the point on
the L-shaped curve rather than somewhere along its vertical arm.
}
\examples{
u <- cobb_douglas(alpha = 0.5)
indifference_curve(u, level = c(2, 4), x = c(1, 2, 4, 8))

# A perfect-complements (Leontief) utility, solved numerically
leontief <- function(x, y) pmin(x, y)
indifference_curve(leontief, level = 3, x = c(3, 4, 5))
}
