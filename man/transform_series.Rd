% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{transform_series}
\alias{transform_series}
\title{Transform a tidy series the way FRED does}
\usage{
transform_series(
  df,
  how = c("lin", "chg", "ch1", "pch", "pc1", "pca", "cch", "cca", "log", "index"),
  frequency = NULL
)
}
\arguments{
\item{df}{A data frame with \code{date} and \code{value} columns and optionally
\code{series_id}, as returned by \code{\link[=fred_series]{fred_series()}}.}

\item{how}{One of the codes above.}

\item{frequency}{Observations per year (1, 4, 12, 52, 260). Inferred from
the dates when \code{NULL}.}
}
\value{
The same frame with \code{value} transformed. Observations that need
an earlier one that does not exist become \code{NA} rather than being
dropped, so the rows still line up with the original.
}
\description{
\code{\link[=fred_series]{fred_series()}} can ask the API for a transformed series through \code{units};
this applies the same definitions locally, so a frame already downloaded
(or one built from any other source) can be reworked without another
request. Each series in the frame is transformed separately.
}
\details{
The codes match FRED's:
\itemize{
\item \code{"lin"} -- levels, unchanged.
\item \code{"chg"} -- change from the previous observation.
\item \code{"ch1"} -- change from a year ago.
\item \code{"pch"} -- percent change from the previous observation.
\item \code{"pc1"} -- percent change from a year ago.
\item \code{"pca"} -- compounded annual rate of change.
\item \code{"cch"} -- continuously compounded rate of change (log difference, in
percent).
\item \code{"cca"} -- continuously compounded annual rate of change.
\item \code{"log"} -- natural log.
\item \code{"index"} -- rescaled so the first non-missing observation is 100.
}

"A year ago" needs the number of observations per year, inferred from the
spacing of \code{date} unless \code{frequency} is given.
}
\examples{
econ <- ggplot2::economics
cpi_like <- data.frame(series_id = "PCE", date = econ$date, value = econ$pce)
head(transform_series(cpi_like, "pc1"), 14)
head(transform_series(cpi_like, "index"))
}
