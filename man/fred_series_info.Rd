% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/series.R
\name{fred_series_info}
\alias{fred_series_info}
\title{Look up FRED series metadata}
\usage{
fred_series_info(series_id, key = fred_key())
}
\arguments{
\item{series_id}{Character vector of FRED series IDs, e.g. \code{"UNRATE"} or
\code{c("UNRATE", "GDPC1")}.}

\item{key}{A FRED API key. Defaults to \code{\link[=fred_key]{fred_key()}}.}
}
\value{
A data frame with one row per series and columns \code{series_id},
\code{title}, \code{units}, \code{units_short}, \code{frequency}, \code{seasonal_adjustment},
\code{observation_start}, \code{observation_end}, \code{last_updated}, \code{popularity} and
\code{notes}.
}
\description{
Returns the title, units, frequency and seasonal-adjustment status of one or
more series -- the information needed to label an axis honestly.
}
\examples{
\dontshow{if (fredscape::fred_has_key()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
fred_series_info("UNRATE")$title
\dontshow{\}) # examplesIf}
}
